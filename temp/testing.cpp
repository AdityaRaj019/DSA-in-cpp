#include <iostream>
#include<sstream>

using namespace std;

int main(){

    string str ;
    cin >> str;
      


    //  stringstream functon to stream the string simply to convert string into stream
    /* What Does stringstream Do?
stringstream is a stream class in C++ that allows us to perform input and output operations on strings (similar to how we use cin and cout for the console).

It is defined in the <sstream> library and helps in:

Extracting data from a string (like reading input from cin).
Formatting data into a string (like writing output to cout).
Parsing structured strings, such as comma-separated values (CSV).*/
}

